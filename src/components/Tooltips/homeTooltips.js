import Tooltip from "react-bootstrap/Tooltip";

export const CVTooltip = (props) => (
  <Tooltip id="button-tooltip" {...props}>
    CV
  </Tooltip>
);

export const LinkedIntooltip = (props) => (
  <Tooltip id="button-tooltip" {...props}>
    LinkedIn
  </Tooltip>
);
export const Githubtooltip = (props) => (
  <Tooltip id="button-tooltip" {...props}>
    Github
  </Tooltip>
);
export const Twittertooltip = (props) => (
  <Tooltip id="button-tooltip" {...props}>
    Twitter
  </Tooltip>
);
